import { test } from "@playwright/test";

test.describe("legacy totem dock", () => {
  test.skip(true, "Totem dock/ghost animation is not part of the current premium product surface.");

  test("legacy totem dock quarantine placeholder", async () => {});
});
